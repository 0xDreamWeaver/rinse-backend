/**
 * Get the metadata for a recently downloaded track. Makes API call to several online
 * services to get the new metadata. Returns the new metadata for the track.
 */
fn get_track_metadata(file_path: &str) -> Result<TrackMetadata, Error> {

}
